import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Frontend Task – Next.js Authentication & Dashboard",
  description: "A modern, responsive Next.js frontend project featuring user authentication flow (Register, Login, Verify), API integration, localStorage token management, and a pixel-perfect UI. Built with ES6+ JavaScript, HTML5, CSS3, Tailwind CSS, and optimized for SSR and SEO",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
